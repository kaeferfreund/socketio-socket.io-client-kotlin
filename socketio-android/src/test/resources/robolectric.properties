# Every Robolectric test runs on SDK 34, 36 and 37 (compileSdk). SDK 35+ needs Robolectric's
# native runtime, which does not exist for Linux on aarch64; the build then limits the run to 34.
sdk=34,36,37
